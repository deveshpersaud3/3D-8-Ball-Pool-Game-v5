function ownsCue(id){

    return player.inventory.cues.includes(id);

}

function ownsAvatar(id){

    return player.inventory.avatars.includes(id);

}

function ownsTitle(id){

    return player.inventory.titles.includes(id);

}